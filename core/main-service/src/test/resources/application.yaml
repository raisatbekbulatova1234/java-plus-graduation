spring:
  cloud:
    config:
      enabled: false
    discovery:
      enabled: false
  sql:
    init:
      mode: always
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
eureka:
  client:
    enabled: false
spring:
  application:
    name: comment-service
  cloud:
    openfeign:
      enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5436/${POSTGRES_DB:comment}
    driver-class-name: org.postgresql.Driver
    username: comment
    password: comment
    init: DEBUG
  sql:
    init:
      mode: always
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: none
    defer-datasource-initialization: true
    open-in-view: false
    show-sql: false

server:
  port: 0

management:
  endpoints:
    web:
      exposure:
        include: "*"

grpc:
  server:
    port: 0
  client:
    collector: # <- должно соответствовать значению в @GrpcClient
      address: 'discovery:///collector' # адрес gRPC-сервиса
      enableKeepAlive: true
      keepAliveWithoutCalls: true
      negotiationType: plaintext
    analyzer: # <- должно соответствовать значению в @GrpcClient
      address: 'discovery:///analyzer' # адрес gRPC-сервиса
      enableKeepAlive: true
      keepAliveWithoutCalls: true
      negotiationType: plaintext
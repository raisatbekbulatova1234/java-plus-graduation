# ============================================================================
# КОНФИГУРАЦИЯ ПО УМОЛЧАНИЮ (application.yml)
# ============================================================================
# Базовые настройки для всех сервисов.
# Конкретные параметры переопределяются в профилях (postgres, test).
# ============================================================================

server:
  port: 0                     # Случайный свободный порт (для избежания конфликтов)
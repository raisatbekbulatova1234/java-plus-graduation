spring:
  application:
    name: collector
grpc:
  server:
    port: 0
collector:
  kafka:
    producer:
      properties:
        bootstrap-servers: localhost:9092
        acks: all
        retries: 3
kafka:
  topic:
    actions: stats.user-actions.v1
